kind = "web"
previewPath = "/"
title = "Vehicle Monitor"
version = "1.0.0"
id = "artifacts/vehicle-monitor"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26125

[services.development]
run = "pnpm --filter @workspace/vehicle-monitor run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vehicle-monitor", "run", "build" ]
publicDir = "artifacts/vehicle-monitor/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26125"
BASE_PATH = "/"
